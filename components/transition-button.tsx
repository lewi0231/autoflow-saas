import React from 'react'
import { Button } from './ui/button'

const TransitionButton = ({type, children}: {type, children: string}) => {

  return (
    <Button></Button>
  )
}

export default TransitionButton
